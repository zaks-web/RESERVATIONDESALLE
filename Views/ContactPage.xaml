<Page x:Class="GantsPlace.Views.ContactPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource SecondaryDarkBrush}" Title="Contact">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>

            <!-- Infos -->
            <StackPanel Grid.Column="0" Margin="0,0,40,0">
                <TextBlock Text="Contactez-nous" FontFamily="{StaticResource AppFont}"
                           FontSize="32" FontWeight="Bold" Foreground="White" Margin="0,0,0,24"/>
                <TextBlock Text="Notre équipe est disponible pour répondre à toutes vos questions concernant la réservation de salles à Lomé."
                           FontFamily="{StaticResource AppFont}" FontSize="15" Foreground="#CCCCCC"
                           TextWrapping="Wrap" LineHeight="24" Margin="0,0,0,24"/>

                <Border Background="{StaticResource PrimaryDarkBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📧" FontSize="24" VerticalAlignment="Center" Margin="0,0,16,0"/>
                        <StackPanel>
                            <TextBlock Text="Email" FontFamily="{StaticResource AppFont}" FontSize="13" Foreground="#AAAAAA"/>
                            <TextBlock Text="contact@gantplace.com" FontFamily="{StaticResource AppFont}" FontSize="15" Foreground="White" FontWeight="SemiBold"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource PrimaryDarkBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📞" FontSize="24" VerticalAlignment="Center" Margin="0,0,16,0"/>
                        <StackPanel>
                            <TextBlock Text="Téléphone" FontFamily="{StaticResource AppFont}" FontSize="13" Foreground="#AAAAAA"/>
                            <TextBlock Text="+228 96 47 07 52 / +229 52 52 69 95" FontFamily="{StaticResource AppFont}" FontSize="15" Foreground="White" FontWeight="SemiBold"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource PrimaryDarkBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📍" FontSize="24" VerticalAlignment="Center" Margin="0,0,16,0"/>
                        <StackPanel>
                            <TextBlock Text="Localisation" FontFamily="{StaticResource AppFont}" FontSize="13" Foreground="#AAAAAA"/>
                            <TextBlock Text="Lomé - TOGO" FontFamily="{StaticResource AppFont}" FontSize="15" Foreground="White" FontWeight="SemiBold"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource PrimaryDarkBrush}" CornerRadius="12" Padding="20">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🕐" FontSize="24" VerticalAlignment="Center" Margin="0,0,16,0"/>
                        <StackPanel>
                            <TextBlock Text="Horaires" FontFamily="{StaticResource AppFont}" FontSize="13" Foreground="#AAAAAA"/>
                            <TextBlock Text="Lun-Ven : 8h-20h  •  Sam : 9h-18h" FontFamily="{StaticResource AppFont}" FontSize="15" Foreground="White" FontWeight="SemiBold"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Formulaire -->
            <Border Grid.Column="1" Background="{StaticResource PrimaryDarkBrush}"
                    CornerRadius="16" Padding="28" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Envoyer un message" FontFamily="{StaticResource AppFont}"
                               FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,16"/>
                    <TextBlock x:Name="TxtSucces" Text="✅ Message envoyé avec succès !"
                               FontFamily="{StaticResource AppFont}" FontSize="14" Foreground="#48C774"
                               Visibility="Collapsed" Margin="0,0,0,8"/>

                    <!-- Nom -->
                    <TextBlock Text="Nom complet" FontFamily="{StaticResource AppFont}" FontSize="13" Foreground="#AAAAAA" Margin="0,0,0,6"/>
                    <Border Background="#3A3A3A" CornerRadius="8" Padding="12,8" Margin="0,0,0,14">
                        <TextBox x:Name="TxtNom" Background="Transparent" BorderThickness="0"
                                 Foreground="White" FontFamily="{StaticResource AppFont}"
                                 FontSize="14" CaretBrush="White"/>
                    </Border>

                    <!-- Email -->
                    <TextBlock Text="Email" FontFamily="{StaticResource AppFont}" FontSize="13" Foreground="#AAAAAA" Margin="0,0,0,6"/>
                    <Border Background="#3A3A3A" CornerRadius="8" Padding="12,8" Margin="0,0,0,14">
                        <TextBox x:Name="TxtEmail" Background="Transparent" BorderThickness="0"
                                 Foreground="White" FontFamily="{StaticResource AppFont}"
                                 FontSize="14" CaretBrush="White"/>
                    </Border>

                    <!-- Sujet - ComboBox sombre -->
                    <TextBlock Text="Sujet" FontFamily="{StaticResource AppFont}" FontSize="13" Foreground="#AAAAAA" Margin="0,0,0,6"/>
                    <Border Background="#3A3A3A" CornerRadius="8" Padding="4,4" Margin="0,0,0,14">
                        <ComboBox x:Name="CmbSujet"
                                  Background="#3A3A3A" Foreground="Black"
                                  BorderThickness="0"
                                  FontFamily="{StaticResource AppFont}" FontSize="14">
                            <ComboBox.Resources>
                                <Style TargetType="ComboBoxItem">
                                    <Setter Property="Background" Value="#3A3A3A"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Style>
                            </ComboBox.Resources>
                            <ComboBoxItem Content="Demande de renseignements" IsSelected="True"
                                          Background="#3A3A3A" Foreground="White"/>
                            <ComboBoxItem Content="Problème avec une réservation"
                                          Background="#3A3A3A" Foreground="White"/>
                            <ComboBoxItem Content="Demande de partenariat"
                                          Background="#3A3A3A" Foreground="White"/>
                            <ComboBoxItem Content="Autre"
                                          Background="#3A3A3A" Foreground="White"/>
                        </ComboBox>
                    </Border>

                    <!-- Message -->
                    <TextBlock Text="Message" FontFamily="{StaticResource AppFont}" FontSize="13" Foreground="#AAAAAA" Margin="0,0,0,6"/>
                    <Border Background="#3A3A3A" CornerRadius="8" Padding="12,8" Margin="0,0,0,16">
                        <TextBox x:Name="TxtMessage" Background="Transparent" BorderThickness="0"
                                 Foreground="White" FontFamily="{StaticResource AppFont}"
                                 FontSize="14" CaretBrush="White"
                                 Height="100" TextWrapping="Wrap" AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"/>
                    </Border>

                    <Button Content="Envoyer le message" Style="{StaticResource PrimaryButtonStyle}"
                            HorizontalAlignment="Stretch" Padding="16,13" Click="BtnEnvoyer_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
