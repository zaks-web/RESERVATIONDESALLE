<Page x:Class="GantsPlace.Views.HistoriquePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource SecondaryDarkBrush}"
      Title="Historique">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Historique des réservations" FontFamily="{StaticResource AppFont}" FontSize="26" FontWeight="Bold" Foreground="White"/>
            <TextBlock x:Name="TxtSoustitre" FontFamily="{StaticResource AppFont}" FontSize="14" Foreground="#AAAAAA" Margin="0,4,0,0"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ListeReservations"/>
        </ScrollViewer>
    </Grid>
</Page>
